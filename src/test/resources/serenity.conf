serenity {
  project.name = "Create User Automation Exercise"
  outputDirectory = "target/site/serenity"

  webdriver {
    driver = "edge"
  }
}

webdriver {
  edgedriver {
    driver = "C:/drivers/msedgedriver.exe"
  }

  timeouts {
    implicitlywait = 5000
  }
}
